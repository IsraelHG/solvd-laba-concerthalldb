package com.solvd.laba.database.dao;

import com.solvd.laba.database.model.Ticket;

/**
 * The TicketDAO interface extends the BaseDAO interface and provides specific data access operations for managing
 * tickets in the database.
 */
public interface TicketDAO extends BaseDAO<Ticket>{
}
